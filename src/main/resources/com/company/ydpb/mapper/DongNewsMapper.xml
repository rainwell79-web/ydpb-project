<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.ydpb.mapper.DongNewsMapper">

    <sql id="criteria">
        <where>
            <choose>
                <when test="searchType == 'T'.toString()">
                    bo_title like '%'||#{keyword}||'%'
                </when>
                <when test="searchType == 'C'.toString()">
                    bo_content like '%'||#{keyword}||'%'
                </when>
            </choose>
        </where>
    </sql>

    <select id="getTotalCount" resultType="int">
        select count(*)
        from tbl_dongnews
        <include refid="criteria" />
    </select>

    <select id="getList" resultType="com.company.ydpb.domain.DongNewsVo">
        select bno, bo_title, bo_content, dept_name, hit, regdate
        from (
            select bno, bo_title, bo_content, dept_name, hit, regdate,
                row_number() over (order by bno desc) as rn
            from /*+ INDEX_DESC(tbl_dongnews SYS_C007218) */
                tbl_dongnews
                <include refid="criteria" />
             )
        where rn between (#{pageNum} - 1) * #{amount} + 1 and #{pageNum} * #{amount}
    </select>

    <select id="getFiles" resultType="com.company.ydpb.domain.FileVo">
        select * from tbl_dongnews_flies where bno = #{bno} order by file_id
    </select>

    <update id="updateCount">
        update tbl_dongnews set hit = hit + 1 where bno = #{bno}
    </update>

    <select id="get" resultType="com.company.ydpb.domain.DongNewsVo">
        select * from tbl_dongnews where bno = #{bno}
    </select>
</mapper>